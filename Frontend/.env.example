# Adónde reenvía el servidor de desarrollo las llamadas a /api.
VITE_BACKEND_URL=http://localhost:3000

# Solo para producción, si el backend vive en otro dominio (ej: https://api.midominio.com/api).
# Vacío = usa /api del mismo dominio.
VITE_API_URL=
